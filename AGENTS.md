# AGENTS.md

## 적용 범위

- 이 파일은 저장소 전체에 적용한다.
- 하위 디렉터리에 `AGENTS.md` 또는 `AGENTS.override.md`가 추가되면 해당 디렉터리에서는 더 가까운 지침을 우선한다.
- 현재 코드와 설정 파일을 프로젝트 상태의 최우선 근거로 사용한다. 문서와 코드가 다르면 임의로 한쪽을 따르지 말고 차이를 보고한다.

## 프로젝트 개요

활로(HWALLO)는 팝업·전시·행사 공간의 배치안을 대피 시뮬레이션으로 사전 검증하고, 위험 발견과 개선·안전 점검을 거쳐 안전 검토 보고서 작성까지 지원하는 시스템이다.

- 핵심 검토 파이프라인: 검토 등록 → 도면·배치 작성 → 시뮬레이션 실행 → 결과 분석
- 결과 분석이 완료되면 해당 결과를 기반으로 AI 보고서 작성 화면으로 이어질 수 있다.
- 위험 예상 항목 관리, 안전 점검 체크리스트, 보고서는 각각 독립적으로 접근 가능한 업무 메뉴다.
- 보고서는 독립 메뉴이지만 시뮬레이션 결과와 연결되며, 결과 분석 화면에서도 작성을 시작할 수 있다.
- 활로 자체에는 결재 또는 승인 기능을 구현하지 않는다.
- 보고서 작성 이후의 결재는 현대퓨처넷의 기존 결재 시스템에서 처리한다.
- AI는 공식 안전 지표를 계산하지 않는다.
- 대피 시간, 밀집도, 병목 등 공식 지표는 시뮬레이션 엔진의 결과를 사용한다.
- AI는 계산된 결과의 해석, 요약 및 보고서 초안 작성에만 사용한다.

## 저장소와 기술 스택

이 저장소는 pnpm과 Turborepo를 사용하는 모노레포다.

- Node.js: 22 이상
- pnpm: 10 이상
- 프론트엔드: React 19, TypeScript, Vite
- 백엔드 공통: Java 17, Spring Boot 3.4, Gradle
- Java 빌드와 실행에는 각 서비스의 Gradle Wrapper를 사용한다.
- 패키지 관리에는 npm이나 yarn 대신 pnpm을 사용한다.

```text
apps/
├─ frontend/
├─ auth-service/
├─ simulation-service/
└─ regulation-service/
```

## 확정된 MSA 서비스 경계

서비스 경계는 다음과 같이 확정한다. 새 기능은 소유 서비스에 구현하며, 사용자 요청 없이 책임을 다른 서비스로 이동하거나 새로운 백엔드 서비스를 추가하지 않는다.

### `apps/frontend`

- 사용자 화면과 클라이언트 상태를 담당한다.
- 기본 개발 포트는 `3000`이다.
- 백엔드의 비즈니스 규칙이나 공식 안전 지표 계산을 클라이언트에 중복 구현하지 않는다.

### `apps/auth-service`

- 인증, 인가, 사용자 및 권한 관리를 담당한다.
- 기본 포트는 `8080`이다.
- 다른 서비스의 시뮬레이션·법령·보고서 데이터를 소유하지 않는다.

### `apps/simulation-service`

- 도면과 공간 배치, 시뮬레이션 설정·실행, 결과 및 위험 분석을 담당한다.
- 기본 포트는 `8081`이다.
- 공식 안전 지표는 시뮬레이션 엔진의 계산 결과를 사용한다.
- 안전 법령과 AI 보고서 작성 책임을 포함하지 않는다.

### `apps/regulation-service`

- 안전 법령 조회·관리, 위험 예상 항목 관리, 안전 점검 체크리스트, AI 기반 보고서 초안 작성 및 보고서 작성을 담당한다.
- 기본 포트는 `8082`이다.
- 공식 안전 지표를 자체 계산하거나 AI 생성값으로 대체하지 않는다.
- 보고서 작성 이후의 결재·승인 상태를 관리하지 않는다.

## MSA 공통 원칙

- 각 서비스는 자신의 데이터와 비즈니스 규칙을 소유한다.
- 다른 서비스의 데이터베이스에 직접 접근하지 않는다.
- 서비스 간 통신은 명시적인 API 또는 이벤트 계약을 통해 수행한다.
- 다른 서비스의 Controller, Service, Repository 또는 내부 도메인 클래스를 직접 참조하지 않는다.
- 서비스 간 요청·응답 DTO와 이벤트 스키마는 소비자가 알 수 있는 명시적 계약으로 관리한다.
- 서비스 간 계약을 변경할 때는 생산자와 소비자 양쪽의 영향을 확인한다.
- 서비스 장애가 연쇄 장애로 번지지 않도록 타임아웃과 실패 처리를 고려한다.
- 현재 저장소에 없는 API Gateway, 서비스 디스커버리, RabbitMQ, Redis, 컨테이너 오케스트레이션 등을 사용자 요청 없이 추가하지 않는다.
- 공통 코드가 실제로 둘 이상의 서비스에서 안정적으로 공유되기 전에는 공용 패키지를 만들지 않는다.
- 한 서비스의 편의를 위해 서비스 경계를 무너뜨리지 않는다.

## 공통 작업 원칙

- 요청한 범위 안에서만 수정한다.
- 관련 없는 코드 정리, 파일 이동 또는 대규모 리팩터링을 함께 수행하지 않는다.
- 수정 전에 대상 서비스의 기존 코드, 설정, 패키지 구조와 스크립트를 확인한다.
- 사용자가 작성한 변경 사항을 덮어쓰거나 되돌리지 않는다.
- 확정되지 않은 기능, API 규격, 데이터베이스 설계 또는 비즈니스 규칙을 임의로 추가하지 않는다.
- 불필요한 추상화와 미래 요구사항을 위한 선행 구현을 피한다.
- 새로운 라이브러리나 외부 서비스를 도입하기 전에 필요성과 기존 대안을 확인한다.
- 비밀키, 비밀번호, 토큰 및 실제 접속 정보를 코드나 Git 추적 파일에 기록하지 않는다.
- `.env` 파일을 커밋하지 않는다. 필요한 환경 변수는 예시 파일이나 문서로 설명한다.
- 오류를 숨기기 위해 타입 검사, lint 규칙 또는 테스트를 비활성화하지 않는다.
- 동작을 검증하지 않은 상태에서 완료했다고 말하지 않는다.

## 프론트엔드 규칙

- React 함수형 컴포넌트와 TypeScript를 사용한다.
- `any` 사용을 피하고 데이터 구조를 명시적인 타입으로 정의한다.
- 화면 상태 타입과 API 요청·응답 타입의 역할을 구분한다.
- 하나의 컴포넌트가 여러 책임을 갖지 않도록 분리한다.
- 화면 전용 코드와 재사용 가능한 컴포넌트를 구분한다.
- 기존 CSS 구성과 디자인 규칙을 먼저 확인하고 따른다.
- 요청받지 않은 UI 라이브러리나 상태 관리 라이브러리를 추가하지 않는다.
- 로딩, 오류, 빈 데이터 상태를 필요한 화면에서 처리한다.
- 접근 가능한 HTML 요소와 적절한 레이블을 사용한다.

## 백엔드 규칙

- 모든 백엔드 서비스는 Java 17을 기준으로 작성한다.
- Java 17에서 지원하지 않는 문법이나 API를 사용하지 않는다.
- 각 서비스의 기본 패키지 경로를 유지한다.
  - `auth-service`: `com.hwallo.auth`
  - `simulation-service`: `com.hwallo.simulation`
  - `regulation-service`: `com.hwallo.regulation`
- Controller에 핵심 비즈니스 로직을 직접 작성하지 않는다.
- HTTP 요청·응답 DTO와 내부 도메인·영속성 객체의 역할을 구분한다.
- 입력값은 신뢰하지 말고 서버에서 검증한다.
- 예외를 무시하거나 빈 `catch` 블록으로 처리하지 않는다.
- 민감한 정보나 전체 요청 본문을 로그에 남기지 않는다.
- 외부 시스템 연결을 코드 실행의 필수 조건으로 만들기 전에 로컬 개발과 테스트 대체 방법을 고려한다.
- 의존성은 필요한 서비스에만 추가한다. 한 서비스의 의존성을 다른 서비스에 일괄 복사하지 않는다.

## 실행 및 검증 명령

저장소 루트의 전체 명령은 다음과 같다.

```bash
pnpm lint
pnpm build
```

프론트엔드만 검증할 때는 다음 명령을 사용한다.

```bash
pnpm --filter @hwallo/frontend lint
pnpm --filter @hwallo/frontend build
```

백엔드 서비스는 변경한 서비스 디렉터리에서 검증한다.

Windows:

```powershell
.\gradlew.bat spotlessCheck
.\gradlew.bat test
```

macOS/Linux:

```bash
./gradlew spotlessCheck
./gradlew test
```

서비스별 pnpm 필터는 다음과 같다.

```text
@hwallo/auth-service
@hwallo/simulation-service
@hwallo/regulation-service
```

## 검증 원칙

- 변경한 영역에 맞는 가장 작은 검증부터 실행한다.
- 프론트엔드를 변경하면 최소한 해당 패키지의 lint와 build를 실행한다.
- 백엔드 서비스를 변경하면 해당 서비스의 `spotlessCheck`와 `test`를 실행한다.
- 공통 설정이나 여러 앱을 변경하면 루트의 `pnpm lint`와 `pnpm build`도 실행한다.
- 테스트가 존재하지 않는 영역은 테스트가 통과했다고 표현하지 않는다.
- 실행할 수 없었던 검증은 명령, 실패 원인과 영향 범위를 함께 보고한다.
- 실패한 검증을 숨기거나 성공한 것처럼 요약하지 않는다.

## 금지 사항

- 사용자 요청 없이 파일, 기능 또는 데이터를 대량 삭제하지 않는다.
- 사용자 요청 없이 기존 API 계약이나 서비스 포트를 변경하지 않는다.
- 사용자 요청 없이 데이터베이스 스키마를 파괴적으로 변경하지 않는다.
- 사용자 요청 없이 패키지 버전을 대규모로 갱신하지 않는다.
- 테스트 통과만을 목적으로 실제 기능을 제거하거나 검증을 약화하지 않는다.
- 활로 내부에 결재·승인 기능을 추가하지 않는다.
- AI 생성값을 공식 안전 지표로 저장하거나 표시하지 않는다.
- 하나의 서비스가 다른 서비스의 데이터베이스나 내부 구현에 직접 의존하게 만들지 않는다.

## 작업 완료 보고

작업을 마칠 때 다음 내용을 간결하게 보고한다.

1. 변경한 내용
2. 변경한 주요 파일
3. 실행한 검증과 결과
4. 실행하지 못한 검증 및 이유
5. 남아 있는 위험이나 후속 작업

검증 결과를 직접 확인하기 전에는 작업이 완료되거나 통과했다고 선언하지 않는다.
