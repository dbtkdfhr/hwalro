# 리뷰 지침

이 저장소의 풀 리퀘스트 리뷰는 다음 기준을 따른다.

## 언어

- 모든 리뷰 코멘트는 한국어로 작성한다.
- 코드 식별자, 파일 경로, 커밋 메시지는 원문을 유지한다.

## 지적 형식

- 발견한 문제는 위치(파일:줄), 문제 설명, 수정 제안 순으로 정리한다.
- 심각도를 `critical` / `high` / `medium` / `low`로 구분한다.
- 단순한 스타일 지적은 수정 가치가 분명할 때만 남긴다.

## 우선 확인 사항

- 활로의 MSA 서비스 경계와 `AGENTS.md` 규칙 위반
- 다른 서비스의 데이터베이스나 내부 구현에 대한 직접 의존
- 공식 안전 지표를 AI 생성값으로 대체하거나 저장하는 코드
- 사용자 요청 없이 추가된 결재·승인 기능, 대규모 패키지 갱신, 파괴적 스키마 변경
- 오류를 숨기는 타입 검사·린트·테스트 비활성화
