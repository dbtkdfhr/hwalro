{
  "name": "@hwalro/auth-service",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../.env -- ./gradlew bootRun",
    "build": "./gradlew compileJava -q",
    "lint": "./gradlew spotlessCheck -q",
    "format": "./gradlew spotlessApply -q",
    "clean": "./gradlew clean -q"
  }
}
