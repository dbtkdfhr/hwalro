# 사용자 관리 서비스 지침

- 사용자 활성화 상태와 역할 변경은 로그인 가능 여부 및 JWT 권한에 직접 영향을 준다.
- `ADMIN`, `OPERATOR`, `SAFETY_REVIEWER` 역할명은 세 서비스의 인가 규칙에서 사용한다. 이름 변경·삭제 시 전체 권한 검사와 초기 데이터를 함께 검토한다.
- 사용자 관리 API의 권한 검사는 서비스 계층에서도 보존하며, 컨트롤러 검증만으로 우회 가능하게 만들지 않는다.
